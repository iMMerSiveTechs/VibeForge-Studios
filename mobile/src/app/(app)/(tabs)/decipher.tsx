/**
 * DecipherKit Tab Page
 * Handwriting transcription interface integrated into VibeForge Studio tabs
 */

import React from "react";
import { DecipherScreen } from "@/decipher";

export default function DecipherTab() {
  return <DecipherScreen />;
}
